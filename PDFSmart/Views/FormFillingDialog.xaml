<Window x:Class="SmartPdfEditor.Views.FormFillingDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SmartPdfEditor.Views"
        xmlns:vm="clr-namespace:SmartPdfEditor.ViewModels"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Background="{DynamicResource AppSurfaceBrush}"
        TextElement.Foreground="{DynamicResource AppInkBrush}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        FontFamily="{DynamicResource AppFontFamily}"
        Title="Fill PDF Form" Height="600" Width="500"
        WindowStartupLocation="CenterScreen">
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Fill out the form fields below:" 
                   Style="{DynamicResource MaterialDesignHeadline6TextBlock}" Margin="0,0,0,15"/>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding FormFields}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="{DynamicResource MaterialDesignDivider}" 
                                BorderThickness="0,0,0,1" Padding="0,10">
                            <StackPanel>
                                <TextBlock Text="{Binding FieldName}" FontWeight="Bold" Margin="0,0,0,5"/>
                                <TextBlock Text="{Binding FieldType, StringFormat='Type: {0}'}" Opacity="0.6" FontSize="11" Margin="0,0,0,5"/>
                                <TextBox Text="{Binding UserValue, UpdateSourceTrigger=PropertyChanged}" 
                                         Style="{DynamicResource MaterialDesignOutlinedTextBox}"
                                         materialDesign:HintAssist.Hint="Enter value"
                                         IsReadOnly="{Binding IsReadOnly}"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Style="{DynamicResource MaterialDesignFlatButton}" Content="CANCEL" 
                    Command="{Binding CancelCommand}" Margin="0,0,10,0" Width="100"/>
            <Button Style="{DynamicResource MaterialDesignRaisedButton}" Content="SAVE" 
                    Command="{Binding SaveFormCommand}" Width="100"/>
        </StackPanel>
    </Grid>
</Window>
